/**
 * Define una interfaz que permita implementar clases capaces de imprimir información por pantalla
 */
export interface Printable{
    /**
     * Imprime información por pantalla.
     */
    print():void;
}